{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run terminal_startup.sh",
            "type": "shell",
            "command": "./terminal_startup.sh",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}